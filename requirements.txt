# CLI Framework
typer

# Beautiful terminal output (tables, progress bars, color)
rich

# Azure SDK
azure-identity
azure-mgmt-resource
azure-mgmt-network
azure-mgmt-compute

# SSH for remote execution
paramiko

# For managing environment variables (secrets)
python-dotenv